// AUTO-GENERATED by `artheia gen-app` — DO NOT EDIT.
// source: odd_path_client/system/components/odd_path_monitor.art
// node:   OddPathMonitor
//
// Slice 2 (stub): runtime wiring. OnStart spawns a thread that loops
// on GwClient::recv_signal() and dispatches by GwMessageHeader fields
// to the right per-port callback. Each frame is nanopb-decoded into a
// `shared_<Pdu>` and handed to the user's impl/<Node>_handlers.cc.

#include "OddPathMonitor.hh"

#include <cstring>

#include <pb.h>
#include <pb_decode.h>

#include "gw_client.h"

namespace odd_path_client {

OddPathMonitor::OddPathMonitor(const OddPathMonitorInputs& inputs)
    : inputs_(inputs) {}

OddPathMonitor::~OddPathMonitor() = default;

void OddPathMonitor::OnCreate() noexcept {
  client_ = std::unique_ptr<GwClient>(new GwClient());
  if (!client_->connect()) {
    inputs_.logger_->error("OddPathMonitor: GwClient connect failed");
  }
}

void OddPathMonitor::OnStart() noexcept {
  if (!client_ || !client_->is_connected()) {
    inputs_.logger_->error("OddPathMonitor: cannot start — not connected");
    return;
  }
  running_.store(true);
  rx_thread_ = std::thread([this]() { this->rx_loop_(); });
  inputs_.logger_->info("OddPathMonitor: rx loop started");
}

void OddPathMonitor::OnStop() noexcept {
  running_.store(false);
  if (rx_thread_.joinable()) rx_thread_.join();
  if (client_) client_->disconnect();
}

void OddPathMonitor::rx_loop_() noexcept {
  GwMessageHeader hdr{};
  uint8_t         proto[256];
  while (running_.load()) {
    ssize_t n = client_->recv_signal(&hdr, proto, sizeof(proto), 200);
    if (n <= 0) continue;

    
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 4u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_ACC_06 msg = mlbevo_gen2_ACC_06_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_ACC_06_fields, &msg)) {
        this->on_acc_06(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0x12eu) {
      shared_ACC_07 msg = shared_ACC_07_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_ACC_07_fields, &msg)) {
        this->on_acc_07(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 4u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_ACC_10 msg = mlbevo_gen2_ACC_10_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_ACC_10_fields, &msg)) {
        this->on_acc_10(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 65u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_AEB_01 msg = mlbevo_gen2_AEB_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_AEB_01_fields, &msg)) {
        this->on_aeb_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 5u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_EML_01 msg = mlbevo_gen2_EML_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_EML_01_fields, &msg)) {
        this->on_eml_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0x104u) {
      shared_EPB_01 msg = shared_EPB_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_EPB_01_fields, &msg)) {
        this->on_epb_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0x106u) {
      shared_ESP_05 msg = shared_ESP_05_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_ESP_05_fields, &msg)) {
        this->on_esp_05(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0x1fcu) {
      shared_ESP_32 msg = shared_ESP_32_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_ESP_32_fields, &msg)) {
        this->on_esp_32(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 5u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_FAS_VK_01 msg = mlbevo_gen2_FAS_VK_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_FAS_VK_01_fields, &msg)) {
        this->on_fas_vk_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 63u
        && hdr.flexray.channel_idx == 0u) {
      shared_GRA_ACC_01 msg = shared_GRA_ACC_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_GRA_ACC_01_fields, &msg)) {
        this->on_gra_acc_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0xadu) {
      shared_Getriebe_11 msg = shared_Getriebe_11_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_Getriebe_11_fields, &msg)) {
        this->on_getriebe_11(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 5u
        && hdr.flexray.channel_idx == 0u) {
      shared_KAS_01 msg = shared_KAS_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_KAS_01_fields, &msg)) {
        this->on_kas_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 65u
        && hdr.flexray.channel_idx == 0u) {
      shared_LDW_02 msg = shared_LDW_02_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_LDW_02_fields, &msg)) {
        this->on_ldw_02(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 17u
        && hdr.flexray.channel_idx == 0u) {
      shared_LWI_01 msg = shared_LWI_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_LWI_01_fields, &msg)) {
        this->on_lwi_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_CAN && hdr.can.can_id == 0x3d6u) {
      shared_Licht_hinten_01 msg = shared_Licht_hinten_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_Licht_hinten_01_fields, &msg)) {
        this->on_licht_hinten_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 51u
        && hdr.flexray.channel_idx == 0u) {
      shared_RCTA_01 msg = shared_RCTA_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, shared_RCTA_01_fields, &msg)) {
        this->on_rcta_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_ObjektHeader msg = mlbevo_gen2_BV2_ObjektHeader_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_ObjektHeader_fields, &msg)) {
        this->on_bv2_objektheader(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_01 msg = mlbevo_gen2_BV2_Objekt_01_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_01_fields, &msg)) {
        this->on_bv2_objekt_01(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_02 msg = mlbevo_gen2_BV2_Objekt_02_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_02_fields, &msg)) {
        this->on_bv2_objekt_02(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_03 msg = mlbevo_gen2_BV2_Objekt_03_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_03_fields, &msg)) {
        this->on_bv2_objekt_03(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_04 msg = mlbevo_gen2_BV2_Objekt_04_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_04_fields, &msg)) {
        this->on_bv2_objekt_04(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 77u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_05 msg = mlbevo_gen2_BV2_Objekt_05_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_05_fields, &msg)) {
        this->on_bv2_objekt_05(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 78u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_06 msg = mlbevo_gen2_BV2_Objekt_06_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_06_fields, &msg)) {
        this->on_bv2_objekt_06(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 78u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_07 msg = mlbevo_gen2_BV2_Objekt_07_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_07_fields, &msg)) {
        this->on_bv2_objekt_07(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 78u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_08 msg = mlbevo_gen2_BV2_Objekt_08_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_08_fields, &msg)) {
        this->on_bv2_objekt_08(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 78u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_09 msg = mlbevo_gen2_BV2_Objekt_09_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_09_fields, &msg)) {
        this->on_bv2_objekt_09(hdr, msg);
      }
      continue;
    }
    if (hdr.bus_type == GW_BUS_TYPE_FLEXRAY
        && hdr.flexray.slot_id == 78u
        && hdr.flexray.channel_idx == 0u) {
      mlbevo_gen2_BV2_Objekt_10 msg = mlbevo_gen2_BV2_Objekt_10_init_zero;
      pb_istream_t stream = pb_istream_from_buffer(proto, (size_t)hdr.proto_len);
      if (pb_decode(&stream, mlbevo_gen2_BV2_Objekt_10_fields, &msg)) {
        this->on_bv2_objekt_10(hdr, msg);
      }
      continue;
    }
    // unmatched: not one of this node's subscribed PDUs; drop silently
  }
}

// ---- clientServer proxies (impl) -------------------------------------------
// One forwarder per (port, operation). The encode/decode + future
// chaining lives in GwClient::async_send_request<Req,Resp>; the proxy
// only carries the service/method ids and the field descriptors.

std::future<gw_status_StatusGetStatusResponse>
OddPathMonitor::status_query_GetStatus(const gw_status_StatusGetStatusRequest& req) {
  return client_->async_send_request<gw_status_StatusGetStatusRequest, gw_status_StatusGetStatusResponse>(
      GW_SVC_STATUS_ID,
      GW_SVC_STATUS_GETSTATUS,
      req,
      gw_status_StatusGetStatusRequest_fields,
      gw_status_StatusGetStatusResponse_fields);
}


}  // namespace odd_path_client
