# Live snapshots are environment state, not source — regenerated by
# `tdb get-snapshot` against a running per. The schemas + transform.json +
# generated plugin are tracked (they're the migration design); snapshots aren't.
snapshots/
