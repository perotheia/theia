{
  "_comment": "L4-B: the V-UCM campaign roster. VucmGate reads boards (comma-list of machine names) and aggregates each board's ucm_activation_<board> PROVISIONAL marker from the shared etcd; CMP_CONFIRMING waits for ALL before fanning the aggregate Confirm. confirm_budget_ms bounds the wait before fanning Cancel.",
  "nodes": {
    "vucm_gate": {
      "boards": "compute",
      "confirm_budget_ms": "120000",
      "bundle_base": "http://10.0.0.99:9000/theia-roles/theia-rig/0.2.1"
    }
  }
}