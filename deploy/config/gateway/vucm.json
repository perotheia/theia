{
  "_comment": "STANDALONE gateway target (rpi4): per-RIG override on top of the generic central machine config. Single-board V-UCM roster — just this board (central:0), so CMP_CONFIRMING waits only on its own ucm_activation marker from the LOCAL etcd (no compute peer). The central machine layer keeps the generic multi-board roster; this override applies for the `gateway` deploy target only.",
  "nodes": {
    "vucm_gate": {
      "boards": "central",
      "confirm_budget_ms": "120000",
      "bundle_base": "http://10.0.0.99:9000/theia-roles/theia-rig/0.2.1",
      "board_instances": "central:0"
    }
  }
}
