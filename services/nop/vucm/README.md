# vucm — not implemented

AUTOSAR Vehicle Update and Configuration Management. A/B partition
hardcore-update is out of scope; we deploy via Puppet (see
`docs/tasks/BACKLOG/etcd-state-backbone.md`).

**Status:** placeholder. `package.art` keeps the TIPC slot only.
