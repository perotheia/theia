// AUTO-GENERATED by `artheia gen-app --kind fc` — DO NOT EDIT.
// source: system/services/shwa/package.art
//
// FC-wide inbound RemoteCodec specializations (#387).
//
// register_call / register_cast on a reporting node's config service
// need a RemoteCodec<T> for every inbound type. This header declares
// ONE THEIA_DECLARE_REMOTE_CODEC per inbound (receiver-port data /
// server-op req+rep) type, deduplicated across EVERY node in the FC.
//
// It lives in one header — not per-node — so that when two nodes in
// the same FC share an inbound type (e.g. com's ComDaemon and the
// test ProbeDaemon both take ComEmpty), the codec specialization is
// declared exactly once even though main.cc includes both node
// headers. Each node header #includes this (guarded by #pragma once),
// so a node header used standalone still has its codecs.
//
// A codec keys its service_id on the proto type name in the message's
// DEFINING package — so an inbound type imported from another package
// (e.g. a PSP bus PDU) declares its codec under that package's name,
// matching the sender. The #include set below covers every defining
// package an inbound type comes from.

#pragma once

#include "RemoteCodec.hh"   // THEIA_DECLARE_REMOTE_CODEC
#include "system/services/shwa/shwa.pb.h"




#ifndef THEIA_CODEC_system_services_shwa_AccelStatusReq
#define THEIA_CODEC_system_services_shwa_AccelStatusReq
THEIA_DECLARE_REMOTE_CODEC(system_services_shwa_AccelStatusReq)
#endif
#ifndef THEIA_CODEC_system_services_shwa_AccelSample
#define THEIA_CODEC_system_services_shwa_AccelSample
THEIA_DECLARE_REMOTE_CODEC(system_services_shwa_AccelSample)
#endif
#ifndef THEIA_CODEC_system_services_shwa_PowerModeReq
#define THEIA_CODEC_system_services_shwa_PowerModeReq
THEIA_DECLARE_REMOTE_CODEC(system_services_shwa_PowerModeReq)
#endif
#ifndef THEIA_CODEC_system_services_shwa_PowerModeReply
#define THEIA_CODEC_system_services_shwa_PowerModeReply
THEIA_DECLARE_REMOTE_CODEC(system_services_shwa_PowerModeReply)
#endif
#ifndef THEIA_CODEC_system_services_shwa_ShwaExcursionEvent
#define THEIA_CODEC_system_services_shwa_ShwaExcursionEvent
THEIA_DECLARE_REMOTE_CODEC(system_services_shwa_ShwaExcursionEvent)
#endif

// FC-wide enum aliases — the enum TYPE and each VALUE inside the FC namespace, so
// impl code writes the readable `PowerMode` / `MODE_UNKNOWN`
// instead of nanopb's double-prefixed name
// (system_services_shwa_<Enum>_<Enum>_<VALUE>). Emitted ONCE here (this header
// is #pragma once + included by every node header) — NOT per-node, so two node
// headers in one TU don't redefine them.
namespace ara::shwa {
using PowerMode = system_services_shwa_PowerMode;
inline constexpr PowerMode MODE_UNKNOWN = system_services_shwa_PowerMode_PowerMode_MODE_UNKNOWN;
inline constexpr PowerMode MODE_MAXN = system_services_shwa_PowerMode_PowerMode_MODE_MAXN;
inline constexpr PowerMode MODE_BALANCED = system_services_shwa_PowerMode_PowerMode_MODE_BALANCED;
inline constexpr PowerMode MODE_LOW = system_services_shwa_PowerMode_PowerMode_MODE_LOW;
}  // namespace ara::shwa
