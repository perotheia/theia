# crypto — not implemented

AUTOSAR `ara::crypto` (Key Storage / Cryptography / X.509 / Key Management)
behind an HSM abstraction. See `docs/autosar/services/crypto.md`.

**Status:** placeholder. No daemon, no real interfaces. The `package.art`
keeps only the TIPC slot reservation so a future implementation can land
without renumbering peers.
