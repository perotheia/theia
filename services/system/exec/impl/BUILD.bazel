# FIRST-TIME-ONLY (regenerated with --force). source: services/system/exec/package.art
#
# Impl slice — handler bodies. The user owns this BUILD.bazel after
# first emit (you may want to add per-FC test_*.cc files; gen-app
# won't reach in and squash that).

load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "exec_impl",
    srcs = ["ExecDaemon_handlers.cc"],
    copts = [
        "-std=c++17",
        "-Wall",
        "-Wextra",
        "-fPIC",
    ],
    deps = [
        "//services/system/exec/lib:exec_lib",
    ],
)
