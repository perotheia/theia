# fw — config-only

AUTOSAR Firewall. In Theia firewalling is iptables rules shipped as
`fw/config/rules.conf` and applied by Puppet — there is no fw daemon.

**Status:** placeholder. `package.art` keeps the TIPC slot only.
