# idsm — not implemented

AUTOSAR Intrusion Detection System Manager (IDSM). Reserved slot,
no daemon today. See `docs/autosar/services/` if/when revisited.

**Status:** placeholder. `package.art` keeps the TIPC slot only.
