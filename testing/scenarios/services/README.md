# Per-FC functional tests

One subdirectory per AUTOSAR Functional Cluster: sm, com, exec, core,
crypto, diag, log, nm, osi, idsm, per, phm, rds, tsync, ucm, vucm,
shwa, fw. Each FC's tests live next to its system/ .art files.
